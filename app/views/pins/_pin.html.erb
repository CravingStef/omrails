<tr>
  <td><%= link_to pin.description, pin %></td>
  <% if current_user == pin.user %>
	  <td><%= link_to 'Edit', edit_pin_path(pin), class: "btn" %></td>
	  <td><%= link_to 'Delete', pin, class: "btn btn-danger btn-small", method: :delete, data: { confirm: 'Are you sure?' } %></td>
	<% end %>
</tr>